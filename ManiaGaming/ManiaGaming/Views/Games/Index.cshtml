
<!DOCTYPE html>
<html>

<head>
    @model ManiaGaming.Models.ProductViewModel;
    @{
        ViewData["Title"] = "Index";
    }
    <link href="https://fonts.googleapis.com/css?family=Permanent+Marker&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Anton&display=swap" rel="stylesheet">
    <title></title>


</head>


<body>



    @*<div class="container-product">
        @foreach (ProductDetailViewModel item in Model.ProductDetailViewModels)

        {
            if (item.Actief == true)
            {
                <div>
                    <a href='@Url.Action("Detail", "Games", new { id = item.Id })'>
                        <div class="containers-products">
                            <img src="~/images/Games.png" alt="Paris">
                            <p class="product-font"> @Html.DisplayFor(modelItem => item.Naam)  </p><p>@Html.DisplayFor(modelItem => item.Prijs),-</p>
                        </div>
                    </a>

                </div>
            }
        }
    </div>*@
    
        <div id="carouselExampleSlidesOnly" class="carousel slide bg-dark" data-ride="carousel">
            <ol class="carousel-indicators">
                <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
            </ol>
            <div class="carousel-inner">
                @foreach (ProductDetailViewModel item in Model.ProductDetailViewModels)

                {
                    if (item.Actief == true)
                    {
                        if (item.Id == 1)
                        {
                            <div class="carousel-item active">
                                <a href='@Url.Action("Detail", "Games", new { id = item.Id })'>

                                    <img src="~/images/Games.png" class="d-block w-100 align-middle" alt="Paris">
                                    <div class="carousel-caption d-none d-md-block">
                                        <p> @Html.DisplayFor(modelItem => item.Naam)  </p><p>@Html.DisplayFor(modelItem => item.Prijs),-</p>
                                    </div>

                                </a>
                            </div>
                        }
                        else if (item.Id > 1)
                        {
                            <div class="carousel-item">
                                <a href='@Url.Action("Detail", "Games", new { id = item.Id })'>

                                    <img src="~/images/Games.png" class="d-block w-100" alt="Paris">
                                    <div class="carousel-caption d-none d-md-block">
                                        <p> @Html.DisplayFor(modelItem => item.Naam)  </p><p>@Html.DisplayFor(modelItem => item.Prijs),-</p>
                                    </div>
                                </a>
                            </div>
                        }
                    }
                }


            </div>
            <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    


</body>

</html>





@*<div id="productContainer">
        <table class="table table-striped table-hover" id="productTable" style="width:100%;">
            <thead>
                <tr>
                    <th>Naam</th>
                    <th>Prijs</th>
                    <th>Acties</th>
                </tr>
            </thead>
            <tbody>
                @foreach (ProductDetailViewModel item in Model.ProductDetailViewModels)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Naam)</td>
                        <td>@Html.DisplayFor(modelItem => item.Prijs)</td>
                        <td>
                            @Html.ActionLink("Detail", "Detail", new { id = item.ProductId })
                    </tr>
                }
            </tbody>
        </table>
    </div>*@
